version: '3.8'

services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-bot
    restart: always
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./.wwebjs_auth:/home/tayadm/github/chatbot/.wwebjs_auth
      - ./combined.log:/home/tayadm/github/chatbot/combined.log
      - ./login.json:/home/tayadm/github/chatbot/login.json
    networks:
      - bot-bridge

networks:
  bot-bridge:
    name: chatbot_shared_network # Nome fixo que não muda com o deploy
    driver: bridge